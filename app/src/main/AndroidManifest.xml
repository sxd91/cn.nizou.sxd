<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 用户头像下载（模块本体独立进程） -->
    <uses-permission android:name="android.permission.INTERNET" />

    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
        </intent>
        <!-- 模块本体（独立进程）查宿主：getLaunchIntentForPackage / getPackageInfo 需包可见性 -->
        <package android:name="com.fenbi.android.leo" />
    </queries>

    <application
        android:name=".AutoOralApplication"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:description="@string/app_description"
        android:multiArch="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.AutoOralCalculation">
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="de.robv.android.xposed.category.MODULE_SETTINGS" />
            </intent-filter>
        </activity>

        <!-- 配置导出/导入的透明承载 Activity（对齐 WeKit TransparentActivity）：
             注入面板是 ComponentDialog 无 ActivityResultRegistry，经本透明 Activity
             启动安卓官方文件选取工具（SAF）。exported=true 以便宿主进程拉起。 -->
        <activity
            android:name=".ui.ConfigTransferActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <!-- 模块设置 Activity（借壳寄生宿主进程，对齐 WeKit SettingsActivity）：
             宿主进程经 ActivityProxy 把本类当宿主 SplashActivity 启动；系统侧按模块 manifest
             校验 ActivityInfo（ActivityProxy 的 PackageManager hook 伪造为宿主包名），
             theme 用模块主题。本类禁止在模块独立进程启动。 -->
        <activity
            android:name=".ui.host.HostSettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.AutoOralCalculation" />
    </application>

</manifest>
